# LCA — Lucien's Chatting App

This build uses Supabase as the permanent server-side database. Render runs the app; Supabase stores accounts, messages, servers, sessions, and the rest of LCA's state.

See `README-SUPABASE.md` for the one-time Supabase/Render setup.
